server:
  port: 18080

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql}:${DB_PORT:3306}/${DB_NAME:wvp}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: ${DB_USER:edge}
    password: ${DB_PASSWORD:edge123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: ${REDIS_HOST:redis}
    port: ${REDIS_PORT:6379}

media:
  id: edge-zlm
  ip: ${ZLM_HOST:zlm}
  http-port: ${ZLM_HTTP_PORT:80}
  secret: RK3576_ZLM_SECRET
  stream-ip: ${ZLM_HOST:zlm}
  hook-ip: ${ZLM_HOST:zlm}
  sdp-ip: ${ZLM_HOST:zlm}

sip:
  ip: 0.0.0.0
  port: ${SIP_PORT:5060}
  domain: 3402000000
  id: 34020000002000000001
  password: 12345678

logging:
  level:
    root: info
